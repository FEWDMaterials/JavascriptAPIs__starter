/* CURRENTLY IN: javascript/main.js */function getGif( tagName ) {	// this is us initializing the process we need	// to do a API call from javascript	var myAJAXCall = new XMLHttpRequest();	// we are describing how this process should work	var APIendpoint = 'http://api.giphy.com/v1/gifs/random?api_key=dc6zaTOxFJmzC&tag='+ tagName;	myAJAXCall.open('GET',APIendpoint );	// we are describing what to do when we get back 	// our data	myAJAXCall.addEventListener('load', function( e ) {		// grab the object string		var responseData = e.currentTarget.response;		// turn it into an actual js object so we can access properties		responseData = JSON.parse( responseData );		// access the image_original_url property		var imageSrc = responseData.data.image_original_url;		console.log( 'this is the payload: ', imageSrc );		var imageElement = document.createElement('img');		imageElement.src = imageSrc;		document.querySelector('#imageHolder').innerHTML = "";		document.querySelector('#imageHolder').appendChild( imageElement );	});	// now we can SEND our request through	// the internets	myAJAXCall.send();}$('.js-search').on('click', function(e){	var searchQuery = $('.js-search-term').val();	getGif(searchQuery);});// getGif('pandas');// getGif('drake');// getGif('shrug');// getGif('shrek');